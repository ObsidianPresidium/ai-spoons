import { RandomForestRegression } from "ml-random-forest";

const rf = new RandomForestRegression({
    nEstimators: 25
});
export const train = () => {
    let trainingSet = [];
    let predictions = [];
}